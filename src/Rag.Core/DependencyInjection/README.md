# Rag.Core Dependency Injection

Service registration extensions for the platform.

`AddRagPlatform(configuration)` should compose parser adapters, chunking strategies, LLM clients, stores, vector search, pipelines, options, and resilience policies.

