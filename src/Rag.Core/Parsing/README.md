# Rag.Core Parsing

Document parser adapters and parser resolution.

Parser support includes `txt`, `md`, and `pdf`. Resolution uses extension or content type and returns normalized text plus metadata.
