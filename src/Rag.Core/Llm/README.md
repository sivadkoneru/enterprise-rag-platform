# Rag.Core LLM

Embedding and chat client abstractions plus provider adapters.

Embedding and chat endpoints are configured separately. `LLM_PROVIDER` selects OpenAI or Azure OpenAI behavior without pipeline changes.

